# A special Testcontainers JDBC URL that auto starts a PostgreSQL container:
spring.datasource.url=jdbc:tc:postgresql:16-alpine:///testdb 

# The Hibernate dialect to use for PostgreSQL
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Tells Spring to use the Testcontainers JDBC driver to interpret the special jdbc:tc: URL
spring.datasource.driver-class-name=org.testcontainers.jdbc.ContainerDatabaseDriver
spring.datasource.username=postgres
spring.datasource.password=postgres

# Ensures Spring doesn’t replace container with H2
spring.test.database.replace=none 

# Hibernate will create the schema at the start of each test run and drop it at the end
spring.jpa.hibernate.ddl-auto=create-drop

# Log all the executed SQL statements to the console.=
spring.jpa.show-sql=true

# Disable logging for cleaner test output
logging.level.org.springframework=WARN
logging.level.com.ubs.tariffapp=INFO

# # Single file mode
# app.data-loader.auto-run=false
# app.data-loader.file-name=clean_HS2017SGYear2023.csv

# # Batch mode
# app.data-loader.batch-mode=true
# app.data-loader.file-list=clean_HS2017SGYear2023.csv,clean_HS2017USAYear2023.csv,clean_HS2017EUYear2023.csv
# app.data-loader.file-directory=clean_data

# # Or for different environments
# # application-dev.properties
# app.data-loader.auto-run=true
# app.data-loader.batch-mode=true
# app.data-loader.file-list=clean_HS2017SGYear2023.csv,clean_HS2017USAYear2023.csv
